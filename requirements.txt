# Ядро: загрузка
yt-dlp>=2024.1.0

# TUI (премиум-интерфейс в терминале)
textual>=0.50.0
rich>=13.0.0

# Auth-слой (браузер для приватных плейлистов).
# На Termux ставится внутри proot-distro/Debian, не в голом Termux.
playwright>=1.40.0
