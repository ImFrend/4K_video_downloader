# Зависимости проекта.
#
# На телефоне руками ставить НИЧЕГО не надо — всё делает одна команда:
#   bash scripts/setup-termux.sh
#
# Этот файл нужен для десктопа/CI:  pip install -r requirements.txt
# Web-UI зависимостей не имеет вообще — он на stdlib (http.server).
# Auth тоже: cookies приезжают из браузера телефона, а проверка сессии —
# один GET через urllib (см. auth/refresh.py). Playwright отсюда убран вместе
# с браузер-слоем: он тянул ~2 ГБ proot+Debian+Chromium и портил состав миксов.

# Ядро: загрузка (нативный Termux)
yt-dlp>=2024.1.0

# TUI (премиум-интерфейс в терминале, нативный Termux)
textual>=0.50.0
rich>=13.0.0
